<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- name space는 MovieRepo의 fully qualified name으로 설정한다. -->
<mapper namespace="com.ssafy.ws.model.dao.MovieDao">

	<!-- Movie에서 모든 정보를 검색한다. -->
	<select id="list" resultType="Movie">
		SELECT * FROM movies
	</select>

	<!-- Movie를 id으로 검색한다. -->
	<select id="search" parameterType="int" resultType="Movie">
		SELECT *
		FROM Movies
		WHERE id = #{id}
	</select>

	<!-- Movie 정보를 저장한다. -->
	<insert id="create" parameterType="Movie">
		INSERT INTO movies (id, title, director, genre, runningtime)
		VALUES(#{id}, #{title}, #{director}, #{genre}, #{runningTime})
	</insert>

	<!-- Movie 정보를 업데이트한다. -->
	<update id="modify" parameterType="Movie">
		UPDATE movies
		SET title = #{title}, director=#{director}, genre = #{genre}, runningtime =
		#{runningTime}, likeCnt = #{likeCnt}
		WHERE id = #{id}
	</update>
	
	<update id="updateCnt" parameterType="Movie">
		UPDATE movies
		SET title = #{title}, director=#{director}, genre = #{genre}, runningtime =
		#{runningTime}, likeCnt = likeCnt + 1
		WHERE id = #{id}
	</update>
</mapper>